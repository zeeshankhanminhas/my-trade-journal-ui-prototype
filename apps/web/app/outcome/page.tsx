import { AppShell } from '@/components/layout/AppShell'; import { SectionCard } from '@/components/ui/SectionCard'; import { outcomeExecuted, skippedOutcome } from '@/lib/mockData';
export default function Page(){return <AppShell title='Outcome Review'><div className='grid gap-3 lg:grid-cols-2'><SectionCard title='Executed Trade'>{Object.entries(outcomeExecuted).map(([k,v])=>k!=='finalOutcome'&&<p key={k} className='text-sm'>{k}: {String(v)}</p>)}</SectionCard><SectionCard title='Skipped Setup'>{Object.entries(skippedOutcome).map(([k,v])=><p key={k} className='text-sm'>{k}: {String(v)}</p>)}<p className='text-sm font-medium mt-2'>Skipped setups are still useful data.</p></SectionCard></div></AppShell>}
