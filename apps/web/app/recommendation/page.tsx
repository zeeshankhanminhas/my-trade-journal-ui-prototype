import { AppShell } from '@/components/layout/AppShell'; import { RecommendationCard } from '@/components/trading/RecommendationCard'; import { ActionChip } from '@/components/ui/ActionChip'; import { recommendationData } from '@/lib/mockData';
export default function Page(){const chips=['Trade Allowed','Wait','Skip','Reduce Risk','Lock','Exit Review'];return <AppShell title='System Recommendation'><div className='space-y-4'><RecommendationCard data={recommendationData}/><div className='flex flex-wrap gap-2'>{chips.map(c=><ActionChip key={c} label={c} active={c==='Wait'}/>)}</div></div></AppShell>}
