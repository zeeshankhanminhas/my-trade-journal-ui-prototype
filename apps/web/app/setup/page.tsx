import { AppShell } from '@/components/layout/AppShell'; import { FormField } from '@/components/ui/FormField'; import { PrimaryButton } from '@/components/ui/PrimaryButton'; import { SectionCard } from '@/components/ui/SectionCard';
export default function Page(){const fields=['Reference Asset','Execution Asset','Setup Type','Gap Direction','Gap Size','Market Regime','Volatility Status','Spread Status','Volume Status','News Risk','Trade Window Start','Trade Window End','Notes'];return <AppShell title='Log Setup' subtitle='Capture setup before emotion enters'><SectionCard><div className='grid gap-3 sm:grid-cols-2'>{fields.map(f=><FormField key={f} label={f}/>)}</div><div className='mt-4 flex flex-wrap gap-2'><PrimaryButton>Save Setup</PrimaryButton><PrimaryButton>Save as Watch Only</PrimaryButton><PrimaryButton>Save as Skipped Setup</PrimaryButton></div></SectionCard></AppShell>}
