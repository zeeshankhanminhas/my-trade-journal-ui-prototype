import { AppShell } from '@/components/layout/AppShell'; import { MetricCard } from '@/components/ui/MetricCard'; import { SectionCard } from '@/components/ui/SectionCard'; import { StatusBanner } from '@/components/ui/StatusBanner'; import { dashboardMetrics,dashboardStatus,recentSetups } from '@/lib/mockData'; import { DecisionLabelPill } from '@/components/trading/DecisionLabelPill';
export default function Page(){return <AppShell title='Dashboard' subtitle='Risk-first operating view'><div className='space-y-4'><StatusBanner title={dashboardStatus.title} message={dashboardStatus.message} severity='trade_allowed'/><div className='grid gap-3 sm:grid-cols-2 lg:grid-cols-5'>{dashboardMetrics.map(m=><MetricCard key={m.label} {...m}/>)}</div><div className='grid gap-3 lg:grid-cols-2'><SectionCard title='Recent Setup'>{recentSetups.map(s=><p key={s.id} className='text-sm'>{s.id} • {s.setupType} • Score {s.systemScore}</p>)}</SectionCard><SectionCard title='Recent Decision Labels'><DecisionLabelPill label='SKIPPED_GOOD_TRADE'/></SectionCard></div></div></AppShell>}
