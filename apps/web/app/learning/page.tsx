import { AppShell } from '@/components/layout/AppShell'; import { SectionCard } from '@/components/ui/SectionCard'; import { learningInsights } from '@/lib/mockData';
export default function Page(){return <AppShell title='Learning'><SectionCard title='Evidence-based Insights'><div className='grid gap-2 sm:grid-cols-2 lg:grid-cols-3'>{learningInsights.map(([k,v])=><div key={k} className='border border-slate-200 rounded-lg p-3'><p className='text-xs text-slate-500'>{k}</p><p className='text-sm font-medium'>{v}</p></div>)}</div><p className='mt-3 text-sm text-slate-600'>The system suggests improvements. Rule changes require user approval.</p></SectionCard></AppShell>}
