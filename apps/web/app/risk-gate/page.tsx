import { AppShell } from '@/components/layout/AppShell'; import { RiskGateCard } from '@/components/trading/RiskGateCard'; import { LockedStateOverlay } from '@/components/ui/LockedStateOverlay'; import { PrimaryButton } from '@/components/ui/PrimaryButton'; import { riskGateChecks } from '@/lib/mockData';
export default function Page(){return <AppShell title='Risk Gate'><div className='space-y-3'><RiskGateCard status='Wait Recommended' checks={riskGateChecks} message='Spread and pending reviews require caution.' allowedNextAction='Wait / Review'/><LockedStateOverlay/><PrimaryButton disabled>Proceed to Trade Action</PrimaryButton></div></AppShell>}
