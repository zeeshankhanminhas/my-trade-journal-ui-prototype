import { recommendationLabels } from '@/lib/constants'; import { SystemRecommendation } from '@/lib/types'; import { SectionCard } from '../ui/SectionCard';
export function RecommendationCard({data}:{data:SystemRecommendation}){return <SectionCard title='System Recommendation'><p className='text-sm text-slate-600 mb-3'>This is a recommendation, not an execution command.</p><div className='grid sm:grid-cols-2 gap-2 text-sm'><p>Setup Score: <b>{data.setupScore}/100</b></p><p>Grade: <b>{data.grade}</b></p><p>Bias: <b>{data.bias}</b></p><p>Recommendation: <b>{recommendationLabels[data.recommendation]}</b></p><p>Allowed Risk: <b>{data.allowedRisk}</b></p><p>Invalidation: <b>{data.invalidationLevel}</b></p></div><p className='mt-2 text-sm'>{data.reason}</p><ul className='mt-2 text-xs list-disc pl-5'>{data.warnings.map(w=><li key={w}>{w}</li>)}</ul></SectionCard>}
