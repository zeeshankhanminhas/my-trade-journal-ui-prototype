'use client'; import { EmotionState } from '@/lib/types'; import { riskyEmotions } from '@/lib/constants';
export function EmotionSelector({value,onChange}:{value:EmotionState;onChange:(v:EmotionState)=>void}){const opts:EmotionState[]=['calm','focused','uncertain','fomo','frustrated','tired','revenge_risk'];return <div><p className='text-sm mb-2'>Emotion State</p><div className='flex flex-wrap gap-2'>{opts.map(o=><button key={o} onClick={()=>onChange(o)} className={`px-3 py-1 rounded-full border text-xs ${value===o?'bg-blue-100 border-blue-300':'bg-white border-slate-300'}`}>{o.replace('_','-').toUpperCase()}</button>)}</div>{riskyEmotions.includes(value)&&<p className='mt-2 text-sm text-amber-800'>Your selected state increases execution risk. Recommended action: WAIT or SKIP.</p>}</div>}
