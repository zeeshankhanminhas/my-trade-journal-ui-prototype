import { RiskGateCheck } from '@/lib/types'; import { SectionCard } from '../ui/SectionCard';
export function RiskGateCard({status,checks,message,allowedNextAction}:{status:string;checks:RiskGateCheck[];message:string;allowedNextAction:string}){const passed=checks.filter(c=>c.passed).length;const failed=checks.length-passed;return <SectionCard title='Risk Gate'><p className='font-medium'>{status}</p><p className='text-sm text-slate-600'>{message}</p><p className='text-sm mt-2'>Passed: {passed} | Failed: {failed} | Next: {allowedNextAction}</p><ul className='mt-2 text-sm space-y-1'>{checks.map(c=><li key={c.name}>{c.passed?'✅':'⚠️'} {c.name} — {c.detail}</li>)}</ul></SectionCard>}
