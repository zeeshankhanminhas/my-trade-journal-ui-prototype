import { DashboardMetric } from '@/lib/types'; import { cn } from '@/lib/utils';
export function MetricCard({label,value,helperText,trend,variant}:DashboardMetric){const v={default:'',success:'border-emerald-200',warning:'border-amber-200',risk:'border-rose-200',neutral:'border-slate-200',locked:'border-rose-300 bg-rose-50'};return <article className={cn('bg-white rounded-xl border shadow-sm p-4',v[variant])}><p className='text-sm text-slate-600'>{label}</p><p className='text-xl font-semibold mt-1'>{value}</p><p className='text-xs text-slate-500 mt-1'>{helperText}</p>{trend&&<p className='text-xs mt-1'>{trend}</p>}</article>}
